schema_version = "1.1"

[task]
name = "kernel-mcp-server/client-capabilities"
description = "Centralize MCP client-capability parsing"
authors = [{ name = "Kernel" }]
keywords = ["mcp", "typescript", "kernel"]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 300.0

[environment]
build_timeout_sec = 900.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
network_mode = "public"
workdir = "/workspace"

[environment.env]

[verifier.env]

[solution.env]
